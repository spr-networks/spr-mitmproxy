// Keep the existing subclass imports working while sharing the same API
// contract and host-injected authentication as every spr-plugin-ui frontend.
export { API as default } from '@spr-networks/plugin-ui'
